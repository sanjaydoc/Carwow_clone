# Secure chat proxy endpoint (your deployed Cloudflare Worker URL).
# The Anthropic API key is NEVER stored here or in the frontend — it lives only
# on the Worker. Copy this file to `.env` and set your Worker URL, then rebuild.
VITE_CHAT_ENDPOINT=
